{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Enables the baseline desktop permissions for the main application window.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
