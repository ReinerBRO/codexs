# Codexs：让 Codex 多账号管理更优雅

## 背景

作为一个重度 Codex 用户，我经常需要在多个项目之间切换，每个项目使用不同的 Codex 账号。传统的做法是手动修改 `~/.codex/auth.json`，然后重启终端会话，非常繁琐。

于是我开发了 Codexs，一个专门用于管理 Codex 账号的桌面应用。

## 核心功能

### 1. 多账号管理

- **一键切换**：点击即可切换账号，无需手动编辑配置文件
- **用量监控**：实时显示每个账号的 5小时/1周 用量窗口
- **智能切换**：自动选择用量最低的账号，避免触发限流
- **自定义标签**：为每个账号设置易记的标签

### 2. 终端会话自动恢复

这是我最喜欢的功能。切换账号时，Codexs 会：

1. 自动检测所有正在运行的 Codex 会话
2. 终止旧账号的会话
3. 在原终端标签页中自动执行 `codex resume <session_id>`

支持 iTerm2 和 Terminal，并且智能跳过 VSCode 终端（避免打断工作流）。

**技术实现：**
- 通过 `ps` 命令找到所有 codex 进程
- 使用 `lsof` 获取进程的工作目录（cwd）
- 通过 AppleScript 在原终端标签页中执行恢复命令
- 遍历父进程链检测 VSCode 终端

### 3. SSH 远程同步

如果你在远程服务器上使用 Codex，这个功能会很有用：

- 切换账号时自动同步 `auth.json` 和 `config.toml` 到远程服务器
- 支持从 `~/.ssh/config` 导入服务器配置
- 支持密钥认证和密码认证

### 4. 账号生成（实验性）

基于 OpenAI 的注册流程，批量生成账号并自动导入到 Codex Tools。

**注意：** 此功能仅供学习研究，请遵守 OpenAI 的服务条款。

## 技术栈

- **前端**：React + TypeScript + Vite
- **后端**：Rust + Tauri
- **UI**：原生 macOS 风格

选择 Tauri 的原因：
1. 性能优秀，内存占用低
2. 原生系统调用能力强（进程管理、AppleScript）
3. 打包体积小

## 使用体验

以前切换账号的流程：
```bash
# 1. 手动编辑配置
vim ~/.codex/auth.json

# 2. 在每个终端中手动恢复会话
codex resume <session_id>
```

现在的流程：
```
点击切换按钮 → 完成
```

所有终端会话自动恢复，远程服务器自动同步，整个过程不到 1 秒。

## 开源

项目已在 GitHub 开源：https://github.com/ReinerBRO/codexs

欢迎提 Issue 和 PR！

## 下载

- GitHub Release: [最新版本](https://github.com/ReinerBRO/codexs/releases)
- 支持 macOS (Apple Silicon)

## 未来计划

- [ ] 支持 Windows 和 Linux
- [ ] 添加账号用量统计图表
- [ ] 支持账号分组管理
- [ ] 添加快捷键支持

---

如果你也在使用 Codex 并且有多账号管理的需求，不妨试试 Codexs。
